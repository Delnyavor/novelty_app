import 'package:flutter/material.dart';

class SampleA extends StatelessWidget {
  const SampleA({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Text: A'),
      ),
    );
  }
}

class SampleB extends StatelessWidget {
  const SampleB({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Text: B'),
      ),
    );
  }
}

class SampleC extends StatelessWidget {
  const SampleC({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Text: C'),
      ),
    );
  }
}

class SampleD extends StatelessWidget {
  const SampleD({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Text: D'),
      ),
    );
  }
}
